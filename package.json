{
  "name": "pve-ups-manager",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && node app.js",
    "dev": "cd backend && node --watch app.js"
  }
}
